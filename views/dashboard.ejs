<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Aplikasi Validasi</title>

    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9"
      crossorigin="anonymous"
    />

    <!-- Bootstrap Icon -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
    />

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap"
      rel="stylesheet"
    />

    <!-- CSS -->
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <!-- Navbar -->
    <nav
      class="navbar navbar-expand-lg navbar-dark align-items-start fixed-top"
    >
      <div class="container">
        <!-- Offcanvas trigger -->
        <button
          class="btn"
          type="button"
          data-bs-toggle="offcanvas"
          data-bs-target="#offcanvasScrolling"
          aria-controls="offcanvasExample"
        >
          <span
            class="navbar-toggler-icon"
            data-bs-target="#offcanvasScrolling"
          ></span>
        </button>
        <!-- Offcanvas trigger end -->
        <a class="navbar-brand me-auto" href="/">Aplikasi Validasi</a>
        <a class="btn btn-light" href="/logout">
          <i class="bi bi-box-arrow-right"></i>
          <span class="d-none d-sm-inline">Logout</span>
        </a>
      </div>
    </nav>

    <!-- Offcanvas -->

    <div
      class="offcanvas offcanvas-start show position-fixed top-0 end-0"
      data-bs-scroll="true"
      data-bs-backdrop="false"
      tabindex="-1"
      id="offcanvasScrolling"
      aria-labelledby="offcanvasScrollingLabel"
      style="margin-top: 56px; width: 250px; z-index: 1050"
    >
      <div class="offcanvas-body">
        <div class="container p-3">
          <ul class="navbar-nav">
            <li>
              <a href="" class="nav-link active">
                <i class="bi bi-laptop me-3"></i>Devices</a
              >
            </li>
            <li>
              <a href="/dashboard/add" class="nav-link">
                <i class="bi bi-plus-circle-fill me-3"></i>Add Device</a
              >
            </li>
            <li>
              <a href="/dashboard/settings" class="nav-link">
                <i class="bi bi-gear me-3"></i>Settings
              </a>
            </li>
            <li>
              <a href="/register" class="nav-link">
                <i class="bi bi-person-add me-3"></i>Add User
              </a>
            </li>
          </ul>
        </div>
      </div>
    </div>
    <!-- Offcanvas end -->

    <!-- Add device button -->
    <div class="container d-flex justify-content-end admin-mt mb-3">
      <a href="/dashboard/add" class="btn btn-1 rounded-pill">Add Device</a>
    </div>

    <!-- Device table -->
    <div class="admin-content me-4 mb-5">
      <table id="deviceTable" class="table table-striped">
        <thead>
          <tr>
            <th scope="col">Serial Number</th>
            <th scope="col">CSM</th>
            <th scope="col">Perangkat</th>
            <th scope="col">Jenis</th>
            <th scope="col">Nama</th>
            <th scope="col">Status</th>
            <th scope="col">Regional</th>
            <th scope="col">NIK</th>
            <th scope="col">Telp</th>
            <th scope="col">Notes</th>
          </tr>
        </thead>
        <tbody>
          <% devices.forEach(device => { %>
          <tr>
            <td><%= device.sn %></td>
            <td><%= device.csm %></td>
            <td><%= device.perangkat %></td>
            <td><%= device.jenis %></td>
            <td><%= device.nama %></td>
            <td><%= device.status === 0 ? 'Tidak Valid' : 'Valid' %></td>
            <td><%= device.regional %></td>
            <td><%= device.nik %></td>
            <td><%= device.telp %></td>
            <td><%= device.notes %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm"
      crossorigin="anonymous"
    ></script>
    <script src="https://code.jquery.com/jquery-3.7.0.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js"></script>
    <script>
      new DataTable("#deviceTable", {
        scrollX: true,
      });
    </script>
  </body>
</html>
